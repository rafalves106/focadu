# Focadu — WhatsApp Service

Implementacao prevista para fase futura.

Servico Node isolado, responsavel por notificacao via WhatsApp. Nao consome nem expoe o dominio
do backend .NET diretamente - integra via API HTTP (`../backend`).
